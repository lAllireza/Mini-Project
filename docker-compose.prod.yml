services:
  backend:
    build:
      target: prod-stage
    environment:
      DJANGO_DEBUG: "False"
    #   DJANGO_SECRET_KEY: ${DJANGO_SECRET_KEY}
    #   DJANGO_ALLOWED_HOSTS: ${DJANGO_ALLOWED_HOSTS}
    # # چون در prod، gunicorn ساده روی HTTP داخل شبکه‌ی داکر گوش می‌دهد (TLS در نگینکس ترمینیت می‌شود)
    # # healthcheck هم دیگر نیازی به https/self-signed cert ندارد.
    # healthcheck:
    #   test:
    #     [
    #       "CMD-SHELL",
    #       "python -c \"import json, urllib.request; resp = urllib.request.urlopen('http://127.0.0.1:8000/api/health/db/', timeout=5); data = json.loads(resp.read()); exit(0 if data.get('status') == 'success' else 1)\""
    #     ]
    #   interval: 15s
    #   timeout: 10s
    #   retries: 3
    #   start_period: 20s

  nginx:
    build:
      target: prod-stage
    volumes:
      - ./NGINX/default.conf:/etc/nginx/conf.d/default.conf:ro